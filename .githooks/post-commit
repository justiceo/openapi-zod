#!/usr/bin/env bash
# Checks for outdated/vulnerable dependencies after every commit and prints
# a maintenance reminder if any are found. Never blocks — post-commit hooks
# can't reject a commit that already happened.
set -euo pipefail

"$(dirname "$0")/dependency-check.sh"
